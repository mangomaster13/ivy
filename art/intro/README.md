# Ivy intro artwork

`source/intro-approved-concept.png` is the approved composition. The other source PNGs separate its night Yard background, `IVY` title with climbing ivy, and the exact line “Come in from the cold.”. Each imageset scale is exported directly from its high-resolution source; the native `PuzzleButton` supplies Enter.

Reference roles: the runtime `story-yard-base@3x.png` anchors the cottage, mailbox, path, matte pixel-storybook painting, and warm/cool light. The former `intro-spread-base@3x.png` anchors only the sparse night tone. The approved concept anchors layout and lettering. The clean background keeps the camera at walking eye level, the mailbox post grounded, and the path clear to the door.

Generation prompts: paint the same Yard cottage at right in a full-bleed 2:1 landscape intro; keep the left sky clear for `IVY` and “Come in from the cold.”. Remove baked lettering and controls for the background export. Make `IVY` with a green vine visibly climbing across its warm-ivory serif letters on transparency. Make the exact tagline as one transparent warm-ivory line. No interface panel or baked button.

The design was approved in conversation. User-owned device testing remains outstanding.
